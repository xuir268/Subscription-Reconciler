# Log

Append-only record of ingests, queries, and lint passes.

## [2026-06-21] setup | Wiki scaffolding created

Initialized `llm-wiki/` with `raw/`, `wiki/`, `index.md`, `log.md`, and the
`CLAUDE.md` schema. No sources ingested yet.
